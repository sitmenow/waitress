const { expect, assert } = require('chai');


suite('TypeORM AdminStore', () => {
  suite('#create()', () => {
    test('First test', () => {
      assert(true);
    });
  });

  suite('#find()', () => {});

  suite('#remove()', () => {});

  suite('#update()', () => {});

  suite('#detail()', () => {});
});
