const { expect, assert } = require('chai');


suite('TypeORM TurnStore', () => {
  suite('#create()', () => {
    test('First test', () => {
      assert(true);
    });
  });

  suite('#find()', () => {});

  suite('#remove()', () => {});

  suite('#update()', () => {});

  suite('#detail()', () => {});
});
