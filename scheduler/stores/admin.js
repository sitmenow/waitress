// TypeORM Admin Store

const Admin = require('scheduler/store/typeorm/admin');


class AdminStore {
  constructor(connection) {
    this.connection = connection;
  }

  create(turn) {
  
  }

  find(turn_id) {
  
  }

  remove(turn_id) {
  
  }

  update(turn) {
  }

  detail(turn) {
  
  }
}

module.exports = AdminStore;
